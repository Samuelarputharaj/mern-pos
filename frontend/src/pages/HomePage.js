import React from 'react';
import ProductList from '../components/ProductList';

const HomePage = () => {
    return (
        <div>
            <h1>POS System</h1>
            <ProductList />
        </div>
    );
};

export default HomePage;
